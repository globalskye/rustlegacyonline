# =============================================
# ВСЕ ПАРОЛИ И НАСТРОЙКИ ПРОДАКШЕНА
# Скопируй в .env и обязательно измени пароли:
#   cp .env.example .env
#   nano .env
# =============================================

# --- База данных PostgreSQL (Adminer использует те же данные) ---
DB_USER=rustlegacy
DB_PASSWORD=ВАШ_НАДЁЖНЫЙ_ПАРОЛЬ
DB_NAME=rustlegacy

# Adminer: https://rustlegacy.online/adminer/
#   Система: PostgreSQL
#   Сервер: postgres
#   Логин: DB_USER
#   Пароль: DB_PASSWORD
#   База: DB_NAME

# --- Nginx ---
REACT_APP_API_URL=/api
NGINX_CONFIG=http

# http = до получения SSL, ssl = после ./deploy.sh ssl
# CERTBOT_EMAIL=admin@rustlegacy.online

# --- SWAG (если 80/443 заняты SWAG) ---
# SWAG_NETWORK=swag
# Имя Docker-сети SWAG (docker network ls). Затем: ./deploy.sh swag

# --- mainnet (инфраструктура Autori: HTTP, порт 80, hostname web.local) ---
# MAINNET=1
# API_PORT=8082   # Порт для плагинов (report, stats sync). По умолчанию 8082

# --- JWT (админка) ---
JWT_SECRET=смените-на-случайную-строку

# --- PayGate.to (платежи: магазин, пополнение баланса) ---
# Документация: 12.txt, https://documenter.getpostman.com/view/14826208/2sA3Bj9aBi
# USDC (Polygon) — ОБЯЗАТЕЛЬНО self-custodial (MetaMask, Trust и т.д.). Адреса бирж отклоняются!
PAYGATE_MERCHANT_WALLET=0x42d14c5e45744d152585CDb7F75c2cA9E67776B8
# URL сайта для callback (без слэша). PayGate шлёт GET на /api/webhooks/paygate?order_id=...
SITE_URL=https://rustlegacy.online

# --- RCON (выдача товаров в магазине) ---
# RCON_HOST=127.0.0.1
# RCON_PORT=28016
# RCON_PASSWORD=пароль_rcon

# --- Синхронизация (TopSystem плагин, http://IP для TLS 1.0) ---
# STATS_SYNC_ENDPOINT=http://62.122.214.201/api/stats/sync

# --- Email (форма обратной связи) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASSWORD=app-password
# SMTP_FROM=support@rustlegacy.online
# CONTACT_EMAIL=support@rustlegacy.online  # куда слать письма (по умолчанию из Company Info)

# --- Аналитика ---
# REACT_APP_GOOGLE_ANALYTICS_ID=G-XXXXXXX
# REACT_APP_YANDEX_METRIKA_ID=XXXXXX
