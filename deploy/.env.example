# Copy to .env and edit
# cp .env.example .env

DB_PASSWORD=your_secure_password_here
DB_NAME=rustlegacy
DB_USER=rustlegacy

# /api for same-origin (nginx proxies)
REACT_APP_API_URL=/api

# http = initial (no SSL), ssl = after cert obtained
NGINX_CONFIG=http

# For Let's Encrypt notifications
CERTBOT_EMAIL=admin@rustlegacy.online

# Optional: Stats sync (POST every 2 min, use http://IP for Rust Legacy TLS 1.0)
# STATS_SYNC_ENDPOINT=http://your-endpoint.com/stats

# Optional: RCON for shop item delivery
# RCON_HOST=127.0.0.1
# RCON_PORT=28016
# RCON_PASSWORD=your_rcon_password

# Optional: JWT secret for admin auth
# JWT_SECRET=change-me-in-production

# Optional: Analytics
# REACT_APP_GOOGLE_ANALYTICS_ID=G-XXXXXXX
# REACT_APP_YANDEX_METRIKA_ID=XXXXXX
