# Production environment for rustlegacy.online
# Copy to .env before: cp .env.production.example .env

# Required - use a strong password
DB_PASSWORD=your_secure_password_here

# Optional overrides
DB_NAME=rustlegacy
DB_USER=rustlegacy

# Frontend API URL - use /api for same-origin (nginx proxies to backend)
REACT_APP_API_URL=/api
