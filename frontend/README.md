# Rust Legacy Server Website

Современный веб-сайт для Rust Legacy сервера с красивым дизайном в сине-белых тонах.

## 🎨 Особенности

- ✨ Современный игровой дизайн в сине-белой цветовой гамме
- 📱 Полностью адаптивный (работает на всех устройствах)
- 🌐 Поддержка двух языков (EN/RU)
- 🎯 Все страницы готовы к работе
- 🔌 API-ready архитектура
- ⚡ Анимации и эффекты на Framer Motion
- 🎮 Игровая эстетика с футуристическими шрифтами

## 📄 Страницы сайта

1. **Главная** (`/`) - Герой секция, новости, особенности сервера
2. **Как начать играть** (`/how-to-start`) - Пошаговая инструкция
3. **Информация о сервере** (`/server-info`) - Детали, плагины, команды
4. **Правила** (`/rules`) - Правила сервера
5. **Магазин** (`/shop`) - Placeholder страница
6. **Статистика** (`/statistics`) - Placeholder страница
7. **Юридические документы** (`/legal/:type`) - Пользовательское соглашение, политика конфиденциальности, информация о компании

## 🚀 Установка и запуск

### Требования
- Node.js 18+ 
- npm или yarn

### Команды

```bash
# Установка зависимостей
npm install --force

# Запуск в режиме разработки
npm start

# Сборка для продакшена
npm run build
```

Сайт откроется на `http://localhost:3000`

## 📁 Структура проекта

```
rust-legacy-site/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   ├── Navigation.tsx    # Навигационное меню
│   │   └── Footer.tsx         # Футер с методами оплаты
│   ├── pages/
│   │   ├── Home.tsx           # Главная страница
│   │   ├── HowToStart.tsx     # Как начать играть
│   │   ├── ServerInfo.tsx     # Информация о сервере
│   │   ├── Rules.tsx          # Правила
│   │   ├── Shop.tsx           # Магазин (placeholder)
│   │   ├── Statistics.tsx     # Статистика (placeholder)
│   │   └── LegalDocument.tsx  # Юридические документы
│   ├── services/
│   │   ├── api.ts             # API сервис
│   │   └── mockData.ts        # Мок-данные
│   ├── types/
│   │   └── index.ts           # TypeScript типы
│   ├── i18n/
│   │   └── config.ts          # Конфигурация переводов
│   ├── App.tsx                # Главный компонент с роутингом
│   ├── App.css                # Основные стили
│   └── index.tsx              # Точка входа
├── package.json
└── tsconfig.json
```

## 🔌 API Endpoints

Все данные берутся из backend API. Пока используются mock-данные из `src/services/mockData.ts`.

### Настройка API URL

В `.env` файле (создайте его в корне проекта):

```env
REACT_APP_API_URL=http://your-backend.com/api
```

### Список endpoints:

#### Server Info
```
GET  /api/server-info          # Получить информацию о сервере
PUT  /api/server-info          # Обновить информацию
```

#### Features
```
GET  /api/features?lang=en     # Получить особенности сервера
POST /api/features             # Создать особенность
PUT  /api/features/:id         # Обновить особенность
DEL  /api/features/:id         # Удалить особенность
```

#### News
```
GET  /api/news?lang=en&published=true  # Получить новости
GET  /api/news/:id                     # Получить одну новость
POST /api/news                         # Создать новость
PUT  /api/news/:id                     # Обновить новость
DEL  /api/news/:id                     # Удалить новость
```

#### Payment Methods
```
GET  /api/payment-methods      # Получить методы оплаты
POST /api/payment-methods      # Создать метод
PUT  /api/payment-methods/:id  # Обновить метод
DEL  /api/payment-methods/:id  # Удалить метод
```

#### Legal Documents
```
GET  /api/legal-documents?lang=en&type=terms  # Получить юридические документы
GET  /api/legal-documents/:id                 # Получить один документ
POST /api/legal-documents                     # Создать документ
PUT  /api/legal-documents/:id                 # Обновить документ
```

#### Server Details
```
GET  /api/server-details?lang=en  # Получить детали сервера
POST /api/server-details          # Создать деталь
PUT  /api/server-details/:id      # Обновить деталь
```

#### Plugins
```
GET  /api/plugins?lang=en      # Получить плагины с командами
POST /api/plugins              # Создать плагин
PUT  /api/plugins/:id          # Обновить плагин
```

#### Getting Started Steps
```
GET  /api/getting-started?lang=en  # Получить шаги "Как начать"
POST /api/getting-started          # Создать шаг
PUT  /api/getting-started/:id      # Обновить шаг
```

#### Players
```
GET  /api/players?online=true  # Получить игроков
GET  /api/players/:steamId     # Получить одного игрока
```

## 📝 Формат данных

### ServerInfo
```json
{
  "id": 1,
  "name": "RUST LEGACY X1",
  "maxPlayers": 100,
  "gameVersion": "Legacy",
  "downloadUrl": "https://example.com/client.zip",
  "virusTotalUrl": "https://virustotal.com/...",
  "descriptions": [
    {
      "id": 1,
      "serverInfoId": 1,
      "language": "en",
      "content": "Server description"
    }
  ]
}
```

### Feature
```json
{
  "id": 1,
  "serverInfoId": 1,
  "language": "en",
  "title": "Feature Title",
  "description": "Feature description",
  "icon": "zap",  // zap, users, globe, shield, server
  "order": 1
}
```

### News
```json
{
  "id": 1,
  "language": "en",
  "title": "News Title",
  "content": "News content",
  "imageUrl": "https://example.com/image.jpg",
  "published": true,
  "publishedAt": "2025-01-15T12:00:00Z",
  "createdAt": "2025-01-15T12:00:00Z",
  "updatedAt": "2025-01-15T12:00:00Z"
}
```

### PaymentMethod
```json
{
  "id": 1,
  "name": "Visa",
  "imageUrl": "https://example.com/visa.png",
  "order": 1,
  "enabled": true
}
```

### LegalDocument
```json
{
  "id": 1,
  "language": "en",
  "type": "terms",  // terms, privacy, rules, company_info
  "title": "Terms of Service",
  "content": "<h3>Section 1</h3><p>Content...</p>",
  "updatedAt": "2025-01-15T00:00:00Z"
}
```

### ServerDetail
```json
{
  "id": 1,
  "language": "en",
  "section": "about",
  "title": "Server Type",
  "content": "<p>Classic x1 server</p>",
  "order": 1
}
```

### Plugin
```json
{
  "id": 1,
  "language": "en",
  "name": "Teleport System",
  "description": "Teleport plugin description",
  "commands": [
    {
      "id": 1,
      "pluginId": 1,
      "command": "/sethome",
      "description": "Set home location",
      "usage": "/sethome [name]"
    }
  ],
  "order": 1
}
```

### GettingStartedStep
```json
{
  "id": 1,
  "language": "en",
  "stepNumber": 1,
  "title": "Download Client",
  "content": "<p>HTML content with instructions</p>",
  "imageUrl": "https://example.com/step1.jpg"
}
```

## 🎨 Цветовая схема

```css
/* Primary Colors */
--primary-blue: #0ea5e9      /* Основной синий */
--accent-cyan: #06b6d4       /* Акцентный голубой */
--accent-teal: #14b8a6       /* Бирюзовый */

/* Background Colors */
--bg-darker: #030712         /* Самый темный фон */
--bg-dark: #0a1628          /* Темный фон */
--bg-card: #0f172a          /* Фон карточек */

/* Text Colors */
--text-primary: #f8fafc     /* Основной текст (белый) */
--text-secondary: #cbd5e1   /* Вторичный текст */
--text-muted: #94a3b8       /* Приглушенный текст */
```

## 🌐 Добавление нового языка

1. Откройте `src/i18n/config.ts`
2. Добавьте новый объект перевода в `resources`:

```typescript
resources: {
  en: { ... },
  ru: { ... },
  de: {  // Новый язык
    translation: {
      nav: {
        home: 'Startseite',
        // ... остальные переводы
      }
    }
  }
}
```

3. Добавьте кнопку переключения языка в `Navigation.tsx`

## 📱 Адаптивность

Сайт полностью адаптивный с breakpoints:
- Desktop: > 1024px
- Tablet: 768px - 1024px  
- Mobile: < 768px

Мобильное меню автоматически активируется на экранах < 1024px.

## 🔧 Кастомизация

### Изменение шрифтов

В `src/App.css` измените импорт Google Fonts:

```css
@import url('https://fonts.googleapis.com/css2?family=YourFont&display=swap');
```

Затем обновите CSS переменные:

```css
body {
  font-family: 'YourFont', sans-serif;
}
```

### Изменение цветов

Все цвета определены как CSS переменные в `src/App.css` в секции `:root`. Просто измените значения этих переменных.

### Добавление новых иконок

Проект использует `lucide-react`. Импортируйте нужные иконки:

```typescript
import { YourIcon } from 'lucide-react';
```

Список всех иконок: https://lucide.dev/icons/

## 🐛 Troubleshooting

### Ошибка при установке зависимостей

```bash
npm install --force
```

### Порт уже занят

Измените порт в `package.json`:

```json
"start": "PORT=3001 react-scripts start"
```

### API не отвечает

Проверьте `.env` файл и убедитесь, что `REACT_APP_API_URL` указывает на правильный адрес.

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `build/`. Загрузите их на ваш веб-сервер (nginx, Apache, etc.)

### Пример конфигурации Nginx

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    root /var/www/rust-legacy/build;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://your-backend:8080;
    }
}
```

## 🇧🇾 Требования законодательства Беларуси

На сайте реализованы все необходимые юридические страницы:

1. **Пользовательское соглашение** (`/legal/terms`)
2. **Политика конфиденциальности** (`/legal/privacy`)
3. **Информация о компании** (`/legal/company`) - с УНП, юридическим адресом, банковскими реквизитами

Обязательно заполните реальные данные вашей компании в backend!

## 🤝 Поддержка

Если возникли вопросы или проблемы, создайте issue в репозитории.

## 📄 Лицензия

MIT License - используйте как хотите!

---

**Готово к использованию! Просто подключите ваш backend API и замените mock-данные.**
