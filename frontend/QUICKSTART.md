# 🚀 Быстрый старт

## Что готово

✅ Полностью рабочий сайт с современным дизайном
✅ 7 страниц (Главная, Как начать, Инфо, Правила, Магазин, Статистика, Юридические)
✅ Поддержка EN/RU языков
✅ Адаптивный дизайн (работает на всех устройствах)
✅ Все API endpoints готовы
✅ Mock-данные для демонстрации
✅ Футер с методами оплаты
✅ Юридические документы для Беларуси

## За 5 минут до запуска

### 1. Установка
```bash
cd rust-legacy-site
npm install --force
```

### 2. Запуск
```bash
npm start
```

Сайт откроется на http://localhost:3000

### 3. Проверьте все страницы
- http://localhost:3000/ - Главная
- http://localhost:3000/how-to-start - Как начать
- http://localhost:3000/server-info - Информация
- http://localhost:3000/rules - Правила
- http://localhost:3000/shop - Магазин
- http://localhost:3000/statistics - Статистика

## Что нужно сделать дальше

### 1. Создайте .env файл
```bash
cp .env.example .env
```

Отредактируйте и укажите URL вашего backend:
```
REACT_APP_API_URL=http://your-server.com/api
```

### 2. Замените mock-данные

Все mock-данные находятся в `src/services/mockData.ts`. Это примеры того, как должен выглядеть ответ вашего API.

**Важно**: Сейчас сайт работает с mock-данными. Когда ваш backend будет готов, просто измените `REACT_APP_API_URL` и всё заработает автоматически!

### 3. Обновите реальные данные

В вашем backend нужно создать endpoints и заполнить:

#### Обязательно для запуска:
- ✅ Server Info (название, описание, ссылка на скачивание)
- ✅ Features (особенности сервера) - минимум 4 штуки
- ✅ Legal Documents (юридические документы) - обязательно для Беларуси!

#### Опционально:
- News (новости)
- Payment Methods (способы оплаты для футера)
- Server Details (детальная информация)
- Plugins (плагины с командами)
- Getting Started Steps (шаги "как начать")

### 4. Юридические документы (ВАЖНО для Беларуси!)

В `src/services/mockData.ts` есть примеры юридических документов. Обязательно замените на реальные данные вашей компании:

- УНП (регистрационный номер)
- Юридический адрес
- Банковские реквизиты
- Контактная информация
- Дата регистрации
- Номер лицензии (если требуется)

## Структура API

Все endpoints описаны в README.md. Основные:

```
GET  /api/server-info
GET  /api/features?lang=en
GET  /api/news?lang=en&published=true
GET  /api/payment-methods
GET  /api/legal-documents?lang=en&type=terms
GET  /api/server-details?lang=en
GET  /api/plugins?lang=en
GET  /api/getting-started?lang=en
```

## Изменение дизайна

### Цвета
Все цвета в `src/App.css` в секции `:root`. Просто измените переменные:

```css
:root {
  --primary-blue: #0ea5e9;     /* Ваш цвет */
  --accent-cyan: #06b6d4;      /* Ваш цвет */
  /* ... и т.д. */
}
```

### Шрифты
В `src/App.css` в начале файла:

```css
@import url('https://fonts.googleapis.com/css2?family=YourFont&display=swap');
```

### Логотип
В `src/components/Navigation.tsx` замените иконку Zap на свою.

## Сборка для продакшена

```bash
npm run build
```

Готовые файлы будут в папке `build/`. Загрузите на ваш сервер.

## Нужна помощь?

1. Проверьте README.md - там полная документация
2. Посмотрите `src/services/mockData.ts` - примеры всех данных
3. Проверьте консоль браузера (F12) на ошибки

## Контрольный чек-лист

- [ ] `npm install --force` выполнен успешно
- [ ] `npm start` запускает сайт без ошибок
- [ ] Все страницы открываются
- [ ] Переключение EN/RU работает
- [ ] Мобильное меню работает
- [ ] Создан .env файл
- [ ] Backend API готов
- [ ] Mock-данные заменены на реальные
- [ ] Юридические документы обновлены
- [ ] Способы оплаты добавлены
- [ ] Сайт собран для продакшена

---

**Всё готово! Просто следуйте инструкциям выше. Удачи! 🎮🚀**
